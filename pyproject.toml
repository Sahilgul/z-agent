[project]
name = "collegium"
version = "0.1.0"
description = "Collegium — ADO-native autonomous engineering platform (uv workspace root)"
requires-python = ">=3.12,<3.13"

[tool.uv.workspace]
members = [
    "backend",
    "worker",
    "packages/contracts",
    "packages/maps",
]

[tool.uv.sources]
collegium-contracts = { workspace = true }
collegium-maps = { workspace = true }
