{
  "name": "collegium-maps-ts",
  "version": "0.1.0",
  "description": "Collegium repo-map generator (TypeScript half) — shared grammar with collegium-maps (Python). Analyzes TS/JS repos via regex/token heuristics. No build step, no runtime deps.",
  "type": "module",
  "bin": {
    "collegium-maps-ts": "src/cli.js"
  },
  "scripts": {
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
