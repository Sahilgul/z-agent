{
  "name": "zagent-maps-ts",
  "version": "0.1.0",
  "description": "Zagent repo-map generator (TypeScript half) — shared grammar with zagent-maps (Python). Analyzes TS/JS repos via regex/token heuristics. No build step, no runtime deps.",
  "type": "module",
  "bin": {
    "zagent-maps-ts": "src/cli.js"
  },
  "scripts": {
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
