# Zagent local-era env — copy to infra/.env and fill. NEVER commit real values.

# --- LLM gateway ---
LITELLM_MASTER_KEY=sk-zagent-master-change-me
GATEWAY_DB_PASSWORD=change-me
AZURE_AI_FOUNDRY_API_KEY=
# Foundry endpoint is set in litellm/config.yaml (api_base) — edit there.

# --- Azure DevOps (TWO PATs, plan §10; service account, NOT your identity) ---
ADO_ORG=bostonhealthai
ADO_PROJECT=Product
FETCH_PAT=          # Code:Read ONLY — golden fetcher
FLEET_PAT=          # Code:Read&Write, service account — workers (push/PR)

# --- App ---
ZAGENT_JWT_SECRET=  # openssl rand -hex 32
ZAGENT_ADMIN_USERNAMES=sahil

# --- Spike: one gateway key is enough (mint via: python -m app.gateway.litellm mint) ---
SPIKE_GATEWAY_KEY=

# --- Tailscale (optional; scripts/setup-tailscale.ps1) ---
# Reusable auth key from tailnet admin (tagged, ephemeral=false). Leave empty
# for one-time interactive login instead.
TS_AUTHKEY=
