# Redis for the event bus + approval streams. Single replica
# with AOF persistence — the streams are rebuildable from the DB, so HA Redis
# is not worth the operational cost at fleet scale.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: collegium-redis
  labels: { app: collegium-redis }
spec:
  replicas: 1
  selector:
    matchLabels: { app: collegium-redis }
  template:
    metadata:
      labels: { app: collegium-redis }
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          args: ["redis-server", "--appendonly", "yes"]
          ports: [{ containerPort: 6379 }]
          volumeMounts:
            - { name: data, mountPath: /data }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: 500m, memory: 512Mi }
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: collegium-redis-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: collegium-redis-data
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests: { storage: 10Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: collegium-redis
spec:
  selector: { app: collegium-redis }
  ports: [{ port: 6379 }]
